apiVersion: apps/v1
kind: Deployment
metadata:
  name: wiki-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wiki-mongo
  template:
    metadata:
      labels:
        app: wiki-mongo
    spec:
      containers:
        - name: wiki-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: wiki-mongo-srv
spec:
  selector:
    app: wiki-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017